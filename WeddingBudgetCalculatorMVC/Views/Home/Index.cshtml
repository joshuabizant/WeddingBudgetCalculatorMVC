@model IEnumerable<WeddingBudgetCalculatorMVC.Models.Budget>



<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>Place to show Food Provider Information</p>




    <div class="container p-3">
        <div class="row pt-4">
            <div class="col-6">
                <h2 class="text-primary">Budgets</h2>
            </div>
            <div class="col-6 text-center">
                <a asp-controller="Home" asp-action="Create" class="btn btn-primary">Create new Budget</a>
            </div>
        </div>
        <br />
        @if (Model.Count() > 0)
        {
            <table class="table table-bordered table-striped" style="width:100%">
                <thead>
                    <tr>
                        <th>
                            Budget Name
                        </th>
                        <th>
                            Budget Amount
                        </th>
                        <th>
                            Remaining Budget
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var service in Model)
                    {
                        <tr>
                            <td width="30%">@service.BudgetName</td>
                            <td width="30%">@service.TotalBudget</td>
                            <td width="30%">@service.RemainingBudget</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>No items created yet</p>
        }
    </div>

</body>
</html>